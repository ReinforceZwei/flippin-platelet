{
  "name": "flippin-platelet",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/sync-build.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.8"
  }
}
